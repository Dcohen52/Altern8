# Function to calculate the average of a list of numbers
define average(nums)
    var sum = 0
    for i = 0 to len(nums)-1 then
        $sum = sum + nums@i
    end
    return sum / len(nums)
end

var num_avg = average(num_list)

print("The average of the numbers in the list is: {num_avg}", "nl")